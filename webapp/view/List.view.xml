<mvc:View
    controllerName="novamart.inventory.inventoryportal.controller.List"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">

    <Page
        id="listPage"
        title="{i18n>appTitle}"
        showNavButton="false">
        
        <content>
            <Table
                id="productList"
                items="{
                    path: 'products>/products'
                }"
                noDataText="{i18n>noDataText}"
                mode="SingleSelectMaster"
                selectionChange=".onProductSelect"
                updateFinished=".onListUpdateFinished"
                growing="true"
                growingThreshold="15">
                
                <headerToolbar>
                    <OverflowToolbar id="tbListHeader">
                        <Title id="txtListTitle" text="{i18n>listTitle}" level="H2"/>
                        <ToolbarSpacer id="spcHeader"/>
                        <SearchField
                            id="searchField"
                            width="40%"
                            placeholder="{i18n>searchPlaceholder}"
                            liveChange=".onSearch"/>
                        <Button
                            id="btnViewSettings"
                            icon="sap-icon://filter"
                            tooltip="{i18n>btnViewSettingsTooltip}"
                            press=".onOpenViewSettings"/>
                        <Button
                            id="btnAddProduct"
                            icon="sap-icon://add"
                            type="Emphasized"
                            text="{i18n>btnAdd}"
                            press=".onAddProduct"/>
                    </OverflowToolbar>
                </headerToolbar>

                <columns>
                    <Column id="colProduct">
                        <Text id="lblColProduct" text="{i18n>colProductName}" />
                    </Column>
                    <Column id="colCategory" minScreenWidth="Tablet" demandPopin="true">
                        <Text id="lblColCategory" text="{i18n>colCategory}" />
                    </Column>
                    <Column id="colPrice" hAlign="End">
                        <Text id="lblColPrice" text="{i18n>colPrice}" />
                    </Column>
                    <Column id="colStatus" hAlign="End">
                        <Text id="lblColStatus" text="{i18n>colStockStatus}" />
                    </Column>
                </columns>

                <items>
                    <ColumnListItem
                        id="cliProduct"
                        type="Navigation"
                        press=".onProductPress">
                        <cells>
                            <ObjectIdentifier
                                id="oiProductName"
                                title="{products>name}"
                                text="SKU: {products>sku}" />
                            <Text id="txtCategory" text="{products>category}" />
                            <ObjectNumber
                                id="numPrice"
                                number="{
                                    parts: [
                                        {path: 'products>price'},
                                        {path: 'products>currency'}
                                    ],
                                    formatter: '.formatter.formatCurrency'
                                }"/>
                            <ObjectStatus
                                id="osListStockStatus"
                                text="{
                                    path: 'products>stock',
                                    formatter: '.formatter.formatStockStatusText'
                                }"
                                state="{
                                    path: 'products>stock',
                                    formatter: '.formatter.formatStockStatusState'
                                }"/>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>